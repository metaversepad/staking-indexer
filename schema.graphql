type User @entity {
  id: ID!
  address: Bytes! # address
  totalValue: BigInt! # amount
}

type StakedEntity @entity {
  id: ID!
  from: Bytes! # address
  amount: BigInt! # uint256
}

type UnstakedEntity @entity {
  id: ID!
  from: Bytes! # address
  amount: BigInt! # uint256
}