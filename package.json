{
  "name": "staking",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ metaversepad/staking",
    "create-local": "graph create --node http://localhost:8020/ metaversepad/staking",
    "remove-local": "graph remove --node http://localhost:8020/ metaversepad/staking",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 metaversepad/staking"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.1",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
